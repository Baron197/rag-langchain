# Dependencies for the evaluation harness (see Dockerfile.eval): the LangChain
# RAG pipeline + Ragas generation metrics. Leaner than requirements.txt (no
# API/UI server libs). Keep versions in sync with requirements.txt.

# LangChain core + hybrid retrieval + providers
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-classic>=1.0
langchain-text-splitters>=0.3
rank-bm25>=0.2
langchain-openai>=0.2
langchain-postgres>=0.0.9

# Config
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Document loading (ingest)
beautifulsoup4>=4.12
pypdf>=4.2

# pgvector driver (eval connects to the same Postgres as the API)
psycopg[binary]>=3.1

# Generation-quality metrics (pulls datasets/pandas transitively)
ragas>=0.1.9,<0.2
datasets>=2.19
